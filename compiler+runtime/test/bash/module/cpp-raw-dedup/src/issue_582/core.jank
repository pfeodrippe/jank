(ns issue-582)

; Test 1: Simple inline function
(cpp/raw "
inline int hello() {
  return 10;
}
")

; Test 2: Multiple functions in same module
(cpp/raw "
inline int world() {
  return 20;
}
")

; Test 3: Duplicate cpp/raw (should be deduplicated)
(cpp/raw "
inline int hello() {
  return 10;
}
")

(defn fn1 []
  (cpp/hello))

(defn fn2 []
  (cpp/world))

(defn fn3 []
  (cpp/hello))

(defn -main []
  (println (fn1))
  (println (fn2))
  (println (fn3)))
